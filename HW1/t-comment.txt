Не воспринимайте тест всерьез :) По поводу точек с запятыми, надо будет пояснить на следующем занятии, напомните пожалуйста :)
Если тест хотите обсудить, напишите в личку пожалуйста в телеграм @S_HappY. Да, в тесте могут быть неточности и ошибки, их стоит исправить...
1 - Вместо parseInt используйте Number.parseInt, с 15 года туда перенесли эту функцию, когда-то обычный parseInt станет устаревшим.
В этом случае, в такой записи как у вас, я бы придрался к тому что выходим за пределы 80 символов в длину строки, потому что есть возможность легко перенести:

const userInputTemper = Number.parseInt(
prompt("Введите температуру в градусах Цельсия: ")
);

Ну и стоит помнить что Number.parseInt дает целое число, пользователь может быть захочет дробное ввести.
userInputTemper переменная - это уже не input пользователя, это уже преобразованное в число значение, по сути это temperC, я бы так и назвал эту переменную.
Что temperC, что temperF не меняются, то есть мы не присваиваем в эти переменные что-то другое, поэтому стоит создать их как const.
Когда выводите расчитываемые значения пользователю - округляйте. Попробуйте ввести 21, посмотрите на результат, мы получим число 69.80000000000001. JavaScript не всегда может "правильно" посчитать некоторые операции, все из-за особенностей рассчетов, которые используются в языке (такое во многих языках встречается). Чтобы округлить число, можно использовать метод Math.round, например:
Math.round(num) округлит до целого числа по правилам математики.
Если мы хотим чтобы округление было до определенного знака после запятой, например, хотим 2 знака после запятой иметь (если они не нули), то можем пойти на хитрость, сначала умножить на 100 число, округлить его и потом поделить на 100:

const PI = 3.14159265359;
console.log(Math.round(PI * 100) / 100); // 3.14

При умножении на 100, мы получаем число 314.159265359, затем округляем по правилам математики, остается 314, после чего делим на 100 и получаем 3.14.
Либо, если мы выводим пользователю результат в строку, то можно превратить число в строку округлив до определенного количества знаков дробной части, например num.toFixed(2) округлит до 2-х чисел в дробной части числа значение переменной num.
2 - Переменные лучше создать с помощью const, т.к. они не меняются в программе.
3 - Идеально. Старайтесь не выходить за пределы 80 символов в строке кода, к комментариям тоже относится.
4 - Шикарно, тут не к чему придраться вовсе. То что строка за 80 символов выходит, это мелочи, к длинным строкам текста я спокойно отношусь.
﻿
Отличная работа! Спасибо :)
